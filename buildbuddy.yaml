actions:
  - name: "Run Unit Tests"
    container_image: "ubuntu-24.04"
    triggers:
      push:
        branches:
          - "main"
      pull_request:
        branches:
          - "*"
    steps:
      - run: "go test ./..."
